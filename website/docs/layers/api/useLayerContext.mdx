---
title: useLayerContext
---

## The Gist

```tsx
import { useLayerContext } from '@weser/layers'

function LayerCount() {
  const { layers } = useLayerContext()

  return <div>Total Layers: {layers.length}</div>
}
```

## Returns

([T_LayerContext](/layers/api/types/T_LayerContext))
