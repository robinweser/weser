---
title: fromZod
order: 1
---

A function that converts a Zod schema to a JSON schema.<br />
For now, it simply uses the [toJSONSchema](https://zod.dev/json-schema) from Zod under the hood.

## The Gist

```ts
import { fromZod } from '@weser/schema'

const schema = z.object({
  name: z.string(),
})

const json = fromZod(schema)
```

## Parameters

| Parameter | Type      | Description                                 |
| --------- | --------- | ------------------------------------------- |
| schema    | `ZodType` | The Zod schema to convert to a JSON schema. |

## Returns

([T_JSONSchema](/schema/api/types/T_JSONSchema)) A JSON schema.
