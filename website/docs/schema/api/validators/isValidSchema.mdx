---
title: isValidSchema
order: 1
---

A function that checks whether a given JSON schema is a valid JSON schema.

## The Gist

```ts
import { isValidSchema, T_JSONSchema } from '@weser/schema'

const schema: T_JSONSchema = {
  type: 'object',
  properties: {
    name: { type: 'string' },
  },
}

const isValid = isValidSchema(schema)
```

## Parameters

| Parameter | Type                                           | Description               |
| --------- | ---------------------------------------------- | ------------------------- |
| schema    | [T_JSONSchema](/schema/api/types/T_JSONSchema) | The JSON schema to check. |

## Returns

(`boolean`) Whether the schema is a valid JSON schema.
